
const initialState = {
  image: '',
  defect: '',
  responsible: '',
  misc: ''
};

export default (state = initialState, action) => {
  switch(action.type){

    default: {
      return state;
    }
  }
}
